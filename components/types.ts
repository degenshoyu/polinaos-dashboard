// components/types.ts
// Shared types used by both left & right panes

export type AnalysisInput = {
  projectName?: string;
  website?: string;
  xProfile?: string;
  xCommunity?: string;
  telegram?: string;
  tokenAddress?: string;
};
